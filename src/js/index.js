import { print } from './helpers';

const numbers = [1, 2, 3];
const numbersDouble = numbers.map((num) => num * 2);

class Person {
   constructor() {
      this.name = 'person';
   }
}

print('texto desde print');

console.log(numbers);
